import 'package:flutter/material.dart';

var lightColors = ColorScheme.fromSeed(
  seedColor: const Color(0xFF2C3E50), // Navy Blue
  brightness: Brightness.light,
  surface: const Color(0xFFF5E4E4),
);
